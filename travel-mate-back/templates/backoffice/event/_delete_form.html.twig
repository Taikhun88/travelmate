<form method="post" action="{{ path('backoffice_event_delete', {'id': event.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet événement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.id) }}">
    <button class="btn">Supprimer</button>
</form>
