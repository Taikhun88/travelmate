{# <form method="post" action="{{ path('backoffice_category_delete', {'id': categories.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet événement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categories.id) }}">
    <button class="btn">Supprimer</button>
</form> #}


<form method="post" action="{{ path('backoffice_category_delete', {'id': categories.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categories.id) }}"><button class="btn btn-outline-danger mb-4 ms-3">Supprimer</button>
</form>